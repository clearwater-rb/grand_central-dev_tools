require "spec_helper"

RSpec.describe GrandCentral::DevTools do
  it "has a version number" do
    expect(GrandCentral::DevTools::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
